# Database Configuration
db.url=jdbc:mysql://localhost:3306/civicpulse?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
db.username=root
db.password=root
db.driver=com.mysql.cj.jdbc.Driver
db.pool.size=10

# JWT Configuration
jwt.secret=CivicPulse2026SecretKeyForJWTTokenGeneration
jwt.expiration=86400000

# File Upload Configuration
upload.directory=/var/www/civicpulse/uploads
upload.max.size=10485760
upload.allowed.extensions=jpg,jpeg,png,gif

# Duplicate Detection Configuration
duplicate.geo.radius.km=0.1

# Application Configuration
app.name=CivicPulse
app.version=1.0.0
app.base.url=http://localhost:8080/civicpulse
